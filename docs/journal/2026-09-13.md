# 2026-09-13 — Project analysis, test plan, and the pitfalls review

(Backfilled from session history; exact times not recorded.)

## What happened

- Full repo analysis of `ls2` (then: docs + validation scripts + a working
  turn-based `live_loop.py`), cross-referenced against LANStreamer's
  robustness plan and test discipline.
- Pitfall review produced 8 findings; the top ones: hidden HuggingFace
  network dependency inside a "no venue internet" system, O(n²)-ish audio
  buffer churn, unhandled device failures, zero tests, no pinned deps,
  no reproducible model setup.
- `docs/test-plan.md` created: automated (U/I/P series) + manual
  (M/R/Q/X series) tests, each with why/expected columns for verification.
- LANStreamer lessons imported: robustness-plan shape ("test for it before
  mitigating it"), hardware-dependent test separation, TODO-as-single-state.

## Lessons

- The biggest bug-in-waiting (runtime HF downloads) was invisible in the
  code review until we asked "what does this do on a machine with no
  internet?" — environment questions find bugs code review misses.
- A test plan written before any tests is where the M-series device-failure
  cases got identified; they later produced 3 real findings in one session.
